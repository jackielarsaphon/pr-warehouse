# pr-warehouse — บันทึกกับดักของโปรเจกต์นี้

เว็บระบบจัดซื้อ (PR/PO/AP/PV) **LIVE ที่ pr.tdlmc.com** · Vue 3 + Vite + Pinia · ฐาน Supabase
· ข้อมูลเอกสารมาจาก TRCloud ผ่าน proxy แล้ว upsert ลง `trcloud_documents`

---

## 🔴 กับดักที่เคยพังจริง — อ่านก่อนแก้โค้ด

### 1. โค้ดมีสองชุด — `src/views/` คือตัวจริง · `views/` ที่รากเป็นโค้ดตาย

`vite.config.js` ตั้ง alias `@` → `./src` และ `src/router/index.js` import `@/views/...`
⇒ **ไฟล์ใน `views/` ที่รากไม่ได้ถูก build เลย** (แก้ล่าสุด 24 มิ.ย. 2026 ขณะที่ `src/views/` แก้ 26 ก.ค.)

เคยวิเคราะห์บั๊กจากไฟล์ผิดชุดมาแล้ว **ตรวจเสมอว่าแก้ไฟล์ใน `src/`**

### 2. ระดับข้อมูลไม่เท่ากันในแต่ละชนิดเอกสาร

`src/stores/trcloud.js` → `pullTypeFromProxy()` ตั้ง `unique_id` ต่างกัน:

| ชนิด | กุญแจ | ได้อะไร |
|---|---|---|
| `po` / `ap` | `item_id` | **1 แถว = 1 บรรทัดสินค้า** |
| `pr` / `pv` | `pr_id` / `payment_id` / `document_number` | 1 แถว = 1 ใบ |

⇒ หน้าเอกสาร (poView/apView) **ต้องใช้ `poDocRows` / `apDocRows`** ไม่ใช่ `poRows` / `apRows`
หน้ารายการสินค้า (trcloudPoItemsView/trcloudApItemsView) ใช้ `*ItemRows` ตามเดิม

**ของจริงที่เคยเกิด (วัด 13 ก.ย. 2026):**
- PO 3,646 แถว = ใบจริง **1,372** ใบ · KPI โชว์ยอด 11,039,681,037 ทั้งที่จริง 4,143,832,628 → **เฟ้อ 2.66 เท่า**
- AP 3,031 แถว = ใบจริง **1,711** ใบ · โชว์ 28,612,190,275 ทั้งที่จริง 19,491,274,515 → **เฟ้อ 1.47 เท่า**
- ใบหนักสุด PO26080180 โผล่ **103 แถว** · AP26060075 โผล่ **100 แถว**

⚠️ `grand_total` เป็นยอด **ทั้งใบ** ที่ซ้ำอยู่ในทุกแถวของใบนั้น — **ห้ามบวกทุกแถว**
(AP26060075: 100 บรรทัด บรรทัดละ 964.7 และ `grand_total` = 96,470 = 100 × 964.7)

### 3. เลขที่เอกสารในข้อมูลดิบ **ไม่มี prefix**

`document_number` = `"26080180"` ส่วน `"PO"` อยู่ที่ `company_format` แยกฟิลด์
- **กุญแจ** ใช้เลขดิบ → `docNumberOf()`
- **แสดงผล** ค่อยต่อ prefix → `docLabelOf()`

(กติกาเดียวกับ WH_K: แสดงต้องมี prefix · กุญแจต้องเลขดิบ)

### 4. ฟิลด์ id ที่ "น่าจะมี" แต่ไม่มีจริง

ตรวจกับข้อมูลจริงแล้ว:
- **PR ไม่มี `pr_id` และไม่มี `id` เลยสักแถว** (2,567 แถว) — ตัวตนจริงคือ `document_number`
- PV มี `payment_id` ครบ 1,233 แถว แต่ **ไม่มี `id`**

⇒ `:key="r.pr_id || r.id"` = `undefined` ทุกแถว Vue ตามแถวไม่ได้
ให้ใช้ `getRowIdentity(r)` เสมอ

### 5. ตัวตนมีสองระดับ อย่าสับสน

| ฟังก์ชัน | ระดับ | ใช้ทำอะไร |
|---|---|---|
| `getRowIdentity(row)` ในแต่ละหน้า | **บรรทัด** (`unique_id`) | ระบบติดตาม `trcloud_tracking.doc_key` |
| `docNumberOf(row, type)` ใน `src/lib/docIdentity.js` | **ใบ** | ยุบแถว · นับใบ · รวมยอด |

🔴 **ห้ามเปลี่ยนนิยาม `getRowIdentity`** — `doc_key` ที่ติดตามไว้เก็บในฐานแล้ว เปลี่ยนเมื่อไรของที่ติ๊กไว้หลุดทันที

### 6. ห้ามใส่ `Math.random()` ในอะไรก็ตามที่ชื่อ `unique_id`

เคยมีใน `buildApItemRow` และ `buildExpenseItemRow` เป็นค่า fallback
⇒ แถวเดิมได้ id ใหม่ทุกครั้งที่ computed คำนวณใหม่ · ยุบซ้ำไม่ได้ตลอดกาล · Vue วาดตารางใหม่ทั้งตาราง
· และการติ๊กติดตามหลุดเพราะ id ไม่ตรงรอบถัดไป

แทนที่ด้วย `contentKey(...)` (FNV-1a) ที่ให้ค่าเดิมเสมอสำหรับเนื้อหาเดิม

### 7. ฐานข้อมูลกันซ้ำอยู่แล้ว — ถ้าเห็นซ้ำ ปัญหาอยู่ที่ชั้นแสดงผล

`trcloud_documents` มี `primary key (doc_type, unique_id)` ⇒ ระดับแถวไม่มีทางซ้ำ
"ซ้ำ" ที่ผู้ใช้เห็นคือ **หน้าระดับใบวาดแถวระดับบรรทัด** เสมอ

---

## ✅ เช็คลิสต์ก่อนเซฟ/deploy

1. แก้ไฟล์ใน `src/` ไม่ใช่ `views/` ที่ราก
2. หน้าระดับใบอ่าน `*DocRows` · หน้ารายการอ่าน `*ItemRows`
3. `:key` ใช้ `getRowIdentity(r)` ห้ามเขียน `r.xxx_id || r.id` ใน template
4. KPI ที่นับใบ/รวมเงิน คิดจากระดับใบ
5. `npx vite build` ต้องผ่าน
6. เทสหน้าเว็บด้วย **Chrome จริง** — stub ที่ใจดีเกินไปคือเทสโกหก
7. ตรวจอัตราส่วนด้วย `docRowRatio(rows, type)` — หน้าระดับใบต้องได้ ratio = 1.0

---

## 🔴 ความปลอดภัยที่ยังค้าง

- `src/stores/trcloud.js` มี **passkey ของ TRCloud เป็นค่า default ในซอร์ส**
  (`import.meta.env.VITE_TRCLOUD_PASSKEY || '<ค่าจริง>'`) ⇒ ถูก bundle ไปกับ JS ที่ส่งให้เบราว์เซอร์
  ควรถอด fallback ออกให้เหลือแต่ env · ถือว่ารหัสนี้รั่วแล้ว
- anon key ของ Supabase อ่าน/แก้ `system_users` ได้ (INSERT ปิดไว้ ต้องใช้ SQL)

## ⚠️ ข้อมูลที่ยังน่าสงสัย

ยอด `grand_total` ของ PR รวมทั้งหมดได้ **880 ล้านล้านกีบ** ซึ่งเป็นไปไม่ได้
ไม่ใช่ปัญหาซ้ำ (ยุบแล้วเท่าเดิม) แต่เป็นสเกลผิดหรือมีขยะปน — ยังไม่ได้ไล่
